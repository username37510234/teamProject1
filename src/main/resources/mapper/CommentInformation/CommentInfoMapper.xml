<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="today.whatdo.festival.festivalinfo.mapper.CommentInfoMapper">

	<!-- 댓글 조회 -->
	<select id="readComment" resultType="today.whatdo.festival.festivalinfo.vo.commentInfo.CommentInfoVO">
    	SELECT
    		CI_NUM, CI_WRITER, CI_CONTENT, CI_REGDATE
    	 FROM COMMENT_INFO
    	<where>
    		CI_ACTIVE = 1
    		<if test ='ciContent != null and ciContent !=""'>
    			AND CI_CONTENT LIKE CONCAT('%',#{CiContent},'%')
    		</if>
    	</where>
	</select>
	
	<select id="selectCommentInfo" resultType="today.whatdo.festival.festivalinfo.vo.commentInfo.CommentInfoVO">
		SELECT
    		CI_NUM, CI_WRITER, CI_CONTENT, CI_REGDATE
    	 FROM COMMENT_INFO
    	WHERE CI_NUM = #{ciNum}
	</select>
	
	<insert id="insertCommentInfo">
		INSERT INTO COMMENT_INFO(
		CI_NUM, CI_WRITER, CI_CONTENT, CI_REGDATE, CI_ACTIVE, 
		FI_NUM)
		VALUES(
		#{ciNum}, #{ciWriter}, #{ciContent}, DATE_FORMAT(NOW(),'%Y%m%d%H%i%s'),#{ciActive}, 
		#{FiNum}
		)
	</insert>
	
	<update id="updateCommentInfoActive">
		UPDATE COMMENT_INFO
		SET CI_ACTIVE=0 
		WHERE CI_NUM=#{ciNum}
	</update>
	
	<update id="updateCommentInfo"> 
		UPDATE COMMENT_INFO
		SET CI_NUM = #{ciNum},
		CI_WRITER = #{ciWriter},
		CI_CONTENT = #{ciContent},
		WHERE CI_NUM=#{ciNum}
	</update>
	
	
	
	<delete id="deleteCommentInfo">
		DELETE FROM COMMENT_INFO WHERE CI_NUM=#{ciNum}
	</delete>
	
	
</mapper>